# 灵剪 AI 4.2 · 智能创作工作台

## 设计依据

参考 DaVinci Resolve Edit 页和 Premiere Pro 的官方工作流：轨道时间线负责结构，检查器负责当前对象参数，效果库提供标题、转场和蒙版；自动化结果始终允许人工覆盖。

## 新增能力

- AI 一键输出可独立看懂的完整叙事时间线，而不是仅拼接高分镜头。
- AI 生成后，镜头可继续波纹删除、刀片分割、边缘裁切、拖动重排、复制和撤销。
- 字幕/标题：中文与英文字体、字号、颜色、底色、透明度、顶部/中央/下三分之一/底部安全区。
- 转场：直接切换、交叉淡化、溶解、左右擦除、左右滑动、圆形展开和平滑推移。
- 蒙版：圆形聚焦、椭圆聚焦、人物卡片和电影宽银幕；支持中心、宽高、羽化和不透明度。
- 当前效果可以先渲染到节目监视器预览，再决定是否导出。
- 工程文件升级为 version 2，并向后兼容旧工程。

## 验证

核心渲染、AI 叙事连续性、精剪交互、UI 冒烟、全部字体/转场/蒙版组合均连续执行 5 轮。
