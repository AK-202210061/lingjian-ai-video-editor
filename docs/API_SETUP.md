# 云端 AI 接口配置

## OpenAI 接口

1. 启动软件，在右侧打开“API 设置”。
2. 接口地址填写 `https://api.openai.com`。
3. 粘贴自己的 API Key。
4. 视觉模型可填写 `gpt-5-mini`，转写模型可填写 `gpt-4o-mini-transcribe`；模型是否可用取决于账户权限。
5. 点击“保存接口设置”，再到“AI 精剪”页运行“云端 AI 内容理解”。

接口调用会产生账户用量。程序仅在主动运行云端分析时上传抽帧联系图与音频，不上传原始完整视频文件。API Key 由 Windows DPAPI 针对当前用户加密，并保存在本机应用设置中；工程文件不保存密钥。

## 兼容接口

可以替换接口地址和模型名称，但服务需要兼容：

- `POST /v1/responses`，支持图像输入和 JSON Schema 结构化输出
- `POST /v1/audio/transcriptions`，支持 multipart 音频转写

若服务只兼容传统 Chat Completions，当前版本不能直接使用。

## 常见问题

- 401/403：密钥错误、权限不足或接口地址不正确。
- 模型不可用：在设置中改为账户实际可用的模型。
- 分析慢：首次需要生成音频和联系图；长视频按素材逐个处理。
- 不希望上传：使用“快速规则初剪”或“离线场景检测”，这两项完全本地运行。
- 公司代理或私有网关：把接口地址改为网关根地址，并确认它保留上述两个路径。
