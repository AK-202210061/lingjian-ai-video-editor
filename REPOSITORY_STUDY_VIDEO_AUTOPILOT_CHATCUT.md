# video-autopilot-kit 与 ChatCut 研究记录

研究日期：2026-08-25  
研究顺序：先 `video-autopilot-kit`，再 ChatCut 原版与 ChatCut 2.0。  
边界：只研究公开源码与公开文档；未复制视频、素材、品牌资产或私有数据，也不把阅读源码称为“训练模型”。

## 研究对象与版本

- video-autopilot-kit：<https://github.com/Hao0321/video-autopilot-kit>
  - 检查提交：`6dc9ad8b3dc9b2ef158e4eac835f5f721e5b3bed`
  - 提交说明：`release: publish v0.21.1 patch`
  - 许可证：MIT
- ChatCut 原版：<https://github.com/akhil-datla/ChatCut>
  - 检查提交：`883b754e5ae6865956ef4be4ca12db1a6d32e973`
  - 许可证：Apache-2.0
- ChatCut 2.0：<https://github.com/dzymachine/ChatCut-2.0>
  - 检查提交：`496b2899c61ac2bd6dc47d11a35407f1a8af79a2`
  - 许可证：Apache-2.0
- ChatCut 产品文档：<https://chatcut.io/docs>

## 一、video-autopilot-kit 学到了什么

这个项目的核心不是“让大模型自由剪片”，而是把剪辑生产拆成可检查、可降级、可回滚的系统。

### 1. 六层思路

1. 根据片型、题材和工作模式选择少量相关知识，避免把整套方法论一次塞给 AI。
2. 素材、脚本、镜头、字幕、效果都要绑定可验证的证据。
3. 创意建议是软规则；真实性、文件安全、技术质量和许可证是硬规则。
4. 自动流程只能通过机械质量门禁，不能自行证明“审美合格”或代替创作者发布。
5. 缺少运动、遮挡、跟踪或镜头关系证据时，复杂转场和特效自动降级为干净切镜。
6. 发布包、版本更新、用户素材和个人偏好分开管理，更新不得覆盖用户数据。

### 2. 对灵剪最有用的机制

- `context_router`：按模式、片型、题材路由少量规则，控制上下文体积。
- `shorts_gate`：把片长、首刀、字幕密度、字幕阅读速度、风险声明证据、首帧质量变成机器规则。
- `autonomy_standard`：无人值守时先修复或降级高风险计划，再进入人工审片队列。
- `quality_95`：机器评分与人工审美明确分开，避免伪造“95 分就是好片”的结论。
- `outcome_learning`：同平台、同观察窗口、足够样本后才形成基线，并明确“相关不等于因果”。
- `release_manager`：逐文件哈希、事务备份、兼容窗口、回滚和用户文件保护。
- `review_loop`：生成带时间码的本地审片包，保留创作者最终决定权。

### 3. 剪辑方法上的可靠规律

- 先建立镜头因果和信息顺序，再谈刺激度与效果。
- 字幕换句可以承担节奏，不应在素材不足时强行增加无意义剪点。
- 第一帧、第一句和第一个动作共同构成钩子；首帧模糊只能警告，不能替代内容判断。
- 字幕停留时间必须随文字长度变化；固定时长字幕会造成不可读。
- 平台规则不可混用，阈值应从自己的成片和留存数据校准。
- 竞品拆解必须区分观察、推算和因果结论，少量成功样本不能产生硬门槛。

### 4. 自检结果与真实限制

执行了仓库自带的 `python src/system_health.py --quick`：

- 通过：编译、路径、上下文路由、模板编译、Shorts、采访规划、质量语料、Quality-95、审片、结果学习、发布契约、存储、架构门禁等核心检查。
- 未通过：`asset-workshop`、`vfx-keyer`、`editorial`、`motion`、`visual-director`、`color`、`tracking`。
- 原因：当前研究环境未安装 Pillow、NumPy、OpenCV。

因此，项目的架构和纯 Python 门禁可直接研究，但视觉模块不是零依赖；“干净安装全绿”需要先按项目要求装齐视觉依赖。

## 二、ChatCut 学到了什么

### 1. 原版 ChatCut

原版是 Adobe Premiere Pro UXP 插件：自然语言先被后端转换成结构化动作，再由插件调用 Premiere API。关键设计包括：

- `AIProvider` 抽象隔离模型供应商。
- `ProcessPromptResponse` 同时支持单动作与批量动作。
- 动作注册表描述参数、默认值和枚举，不允许模型直接生成任意代码。
- `actionDispatcher` 把动作名映射到受控实现。
- 操作前捕获状态，并为缩放、滤镜、转场、模糊提供撤销逻辑。
- 模糊指令可进入澄清流程，而不是猜一个参数直接执行。

原版的限制也很明显：对象跟踪文件明确是占位实现，旧的 MVP 文档仍写着部分时间线效果未完成；它更适合作为“自然语言动作协议”的参考，不适合直接当作成熟剪辑内核。

### 2. ChatCut 2.0

2.0 把概念扩展成 Premiere 插件和独立 Tauri/Next.js 编辑器共用后端。最有价值的新增设计是：

- 统一、可 JSON 序列化的 `Project / Track / Clip / Transform / Effect` 数据模型。
- 秒作为统一时间单位，原素材区间与时间线位置分离。
- 17 个显式工具：读取时间线、读取素材、查询片段、添加/删除/裁剪/移动片段、应用/修改/关闭效果、发现 FFmpeg 滤镜、组合与验证配方。
- 最多 8 轮的供应商无关工具调用循环，支持 Anthropic、Groq、Gemini。
- 每次变更形成可撤销命令，并进入分支式编辑历史 DAG，可回到旧节点进行 A/B 尝试。
- FFmpeg 效果使用声明式 Recipe AST，先检查空配方、重复 ID、悬空引用和环，再编译。
- 浏览器探测媒体失败时，桌面版回退到 FFprobe。

### 3. 不能照搬的部分

- 模型直接调用修改工具，尚未形成 video-autopilot-kit 那样完整的“分析—计划—机械门禁—预览—确认—发布”边界。
- 工具参数结构虽然明确，但部分对象参数仍较宽，不能代替逐动作业务校验。
- Recipe 允许 `raw` FFmpeg 字符串；在正式软件中必须做严格白名单、转义和资源上限控制。
- 当前仓库只有很少的前端自动化测试，路线图也明确把完整测试夹具、外部 MCP 实时编辑、部分转场和关键帧能力放在后续阶段。
- 对象跟踪仍有占位实现，不能视为可交付能力。
- 原版和 2.0 的文档存在历史状态混杂，必须以当前代码与可运行测试为准。

## 三、两者组合后，灵剪应该采用的架构

### A. 决策面：借鉴 video-autopilot-kit

`素材分析 → 故事意图 → 候选片段 → 顺序/因果门禁 → 节奏与字幕门禁 → 效果证据门禁 → 待审方案`

- 用户标记“按拍摄顺序”时，钩子之后的主体镜头只能按真实拍摄时间单调递增。
- AI 只能从候选片段生成 Edit Decision List，不能直接破坏项目。
- 没有镜头关系证据的转场降级为硬切或短叠化。
- 每条字幕检查阅读速度、安全区、遮挡和内容证据。
- 120 秒项目按章节规划，而不是把 30 秒规则机械拉长。

### B. 执行面：借鉴 ChatCut 2.0

`受控工具注册表 → 参数校验 → 预览差异 → 原子执行 → Undo Command → Edit History DAG`

建议灵剪新增的首批工具：

1. `inspect_timeline`
2. `inspect_selected_clips`
3. `propose_rough_cut`
4. `trim_clip`
5. `split_clip`
6. `remove_clip`
7. `move_clip_with_order_guard`
8. `add_caption_region`
9. `apply_transition_with_evidence`
10. `apply_mask`
11. `normalize_dialogue`
12. `validate_export`

所有修改工具都应返回：变更摘要、受影响片段、旧值、新值、警告、可撤销 ID。

### C. 创作者体验

- AI 先展示“为什么选这些片段”和“删掉了什么”，用户确认后落到时间线。
- 时间线中的 AI 操作形成独立历史节点，可关闭、重新启用、删除或创建分支。
- 对含糊指令只问一个最关键问题；能从当前选择、播放头和项目设置确定的参数不再追问。
- 模型供应商放在统一接口后，API 欠费或不可用时仍保留本地剪辑、规则规划和导出。

## 四、与灵剪 4.3.0 当前实现的差距

灵剪已经具备可继续演进的基础：

- `ai_api.py` 已有拍摄顺序锁、叙事模式、故事蓝图和连续性整理。
- `editing_profile.py` 已有本地个人偏好。
- `timeline_widget.py` 和 `app_v2.py` 已有可操作时间线、撤销/重做和导出入口。
- `core.py` 已有媒体探测、场景检测、质量扫描与导出验证。

下一阶段最关键的缺口不是再堆转场，而是：

1. 把 AI 规划结果改为版本化、可校验的 EDL/工具协议。
2. 为每次 AI 修改建立原子 Undo 与分支历史。
3. 在 60/120 秒规划中加入章节级顺序与因果门禁。
4. 把字幕、音频、遮罩、转场和导出分别纳入机器 QA。
5. 建立“本地规则可用、云 AI 可选”的降级路径。
6. 给所有宣称可用的功能增加真实媒体回归测试，文档不得代替测试。

## 五、许可证结论

- video-autopilot-kit 为 MIT，可借鉴和修改，但分发时须保留版权和许可文本。
- ChatCut 两个仓库为 Apache-2.0，可借鉴和修改；分发衍生代码时须附许可证、保留相关声明，并明确标注修改过的文件。
- 品牌名称、商标、第三方素材和外部服务条款不随源码许可证自动授权。
- 灵剪应优先重写适合自身 PySide/FFmpeg 架构的模块；若直接复用源码，必须在 `THIRD_PARTY_NOTICES.md` 做文件级归属记录。

## 最终判断

最适合灵剪的路线不是照搬任一仓库：

- 用 video-autopilot-kit 约束“AI 为什么这样剪、什么时候必须降级、如何验证与发布”。
- 用 ChatCut 2.0 设计“AI 如何通过有限工具修改时间线、如何撤销、如何分支试错”。
- 保留灵剪现有的真实拍摄顺序学习、中文界面、本地偏好、PySide 时间线和 player-safe MP4 导出。

这样才能从“AI 一键生成一次结果”升级成“AI 提方案，创作者可精细修改，系统始终可解释、可撤销、可验证”的编辑器。
