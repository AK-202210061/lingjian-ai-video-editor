# 灵剪 AI 4.12.0

- 工程格式升级为 v4：新增带绝对时间线位置的独立叠加片段，旧工程自动按单 V1 轨兼容打开。
- AI 会根据高级开场复杂度创建 0–2 条 V2/V3 轨；Q 弹回溯、走马灯、动态分屏等可以获得真正独立的合成层。
- 新增“多轨合成”页，可手工创建 V2–V6，修改轨道、时间、持续时间、布局、蒙版和透明度，也可删除。
- 时间线动态显示 V2/V3/V4…，轨道数增加时启用垂直滚动，V1、字幕和音频轨仍保留。
- FFmpeg 渲染器按 V1 → V2 → V3… 顺序逐层合成，支持椭圆、圆形和矩形画中画；叠加层超过 V1 时自动延长底层画面与音频。
- AI 方案预览明确列出每个叠加层的轨道、素材、出现时间、蒙版和用途。
- 导出前检查叠加素材缺失、入出点越界、轨道参数无效和画面层数过载。

多轨用于服务叙事与蒙版，不会改变钩子后的真实拍摄顺序锁。
