# 灵剪 AI 视频编辑器 4.0 测试报告

测试日期：2026-08-24

## 新增验证

- AI 导演候选分析、全局顺序规划、失败回退、重复抑制、字幕换行和标准像素格式：5 轮、50 项检查，通过
- 3.0 精剪界面/API/DPAPI 兼容回归：5 轮、110 项检查，通过
- 三段 DJI HEVC Main 10 素材离线导演：全部素材覆盖、5 个镜头、20.0 秒、无短于 1.2 秒片段，通过
- 三段 DJI 20 秒实际样片：H.264 High、8-bit yuv420p、AAC、360×640、20.0 秒，通过
- 分析代理建立后，同一组三段素材离线选段约 2.75 秒

## 修复

- 导出明确强制 `yuv420p`，防止 10-bit DJI 原片让 H.264 输出继承为兼容性较差的 High 10。
- HEVC AI 分析优先使用已生成的 H.264 代理，但所有切点保持原始时间码，最终仍从原片渲染。
- AI 不再分别打分后按导入顺序拼接；新增全局导演决策列表。

## 边界

- 未使用用户的付费 API Key进行线上扣费测试；Responses 与 Transcriptions 请求通过本机兼容服务五轮验证。
- AI 初剪是可编辑草稿，不保证在所有题材上等同专业剪辑师；素材质量、提示词、模型能力和 API 额度会影响结果。
