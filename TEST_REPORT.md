# 灵剪 AI 1.0 测试报告

测试日期：2026-08-23

## 结果

- 核心功能回归：5/5 轮通过，85 个检查点。
- Qt 界面与状态联动：5/5 轮通过，40 个检查点。
- 打包后 EXE 启动：5/5 轮通过。
- FFmpeg 引擎：7.1，可在发布包内独立执行。
- 实际导出：5/5 个 H.264/AAC MP4 通过媒体探测，均包含音轨。

## 每轮覆盖

媒体探测、视频尺寸与时长、原声识别、无声素材兼容、缩略图、添加片段、裁切、非法裁切拒绝、分割、排序、删除、中文字幕、字幕位置、淡入淡出、背景音乐混合、工程保存恢复、AI 目标时长初剪、FFmpeg 实际导出与成品复检。

## 测试中发现并修复

1. `ffmpeg-static` 提供的二进制在当前 Windows 环境无法启动，替换为经过执行验证的 Gyan FFmpeg 7.1 Windows x64 构建。
2. FFmpeg 7.1 不接受 `.2` 形式的淡入淡出时长，修正为 `0.2`。
3. AI 初剪在目标剩余时间不足 2 秒时可能超出目标，修正选择上限逻辑。
4. 测试进程读取 FFmpeg 输出时受系统 GBK 编码影响，固定为 UTF-8 并容错解码。

## 已知边界

离线 1.0 的 AI 初剪为本地规则引擎，不含需要下载大型模型或调用云服务的语义识别、生成式音效和语音转写。程序不会上传用户素材。
